export enum AcEnumSelectionMode {
  None = 'none',         // No row can be selected
  Single = 'single',     // Only one row can be selected at a time
  Multiple = 'multiple', // Multiple rows can be selected
}
