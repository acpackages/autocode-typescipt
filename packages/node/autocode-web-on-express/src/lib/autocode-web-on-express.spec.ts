import { autocodeWebOnExpress } from './autocode-web-on-express';

describe('autocodeWebOnExpress', () => {
  it('should work', () => {
    expect(autocodeWebOnExpress()).toEqual('autocode-web-on-express');
  });
});
